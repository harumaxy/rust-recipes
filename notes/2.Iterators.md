# Iterator

関連タイプ Item

- next() : 先頭のitemを返す(Option)
- last() : 最後のitemを返す(Option)

# IntoIterator trait

この trait を実装すると、コレクション系のオブジェクトを iterator にできる
- `.into_iter()`
- `.iter_mut()`
- `.iter()`

## 新しいライブラリ
